version: '3'

services:
    redis_dev:
        container_name: redis_dev
        image: redis
        ports: 
            - 6379:6379
        volumes: 
            - /etc/redis:/usr/local/etc/redis/
            - /data/redis/data:/data
        restart: unless-stopped
    